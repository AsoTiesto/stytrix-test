# 測試文件

這份文件記錄 L1～L5 的完整測試流程與結果。測試分成三層：Playwright 開真瀏覽器跑的 E2E 測試（`test/e2e.mjs`）、直接對 WebSocket 協定層的同步測試（`test/sync-protocol.mjs`），以及只能由真人操作的手動驗證。

## 怎麼跑

```bash
npm install
npx playwright install chromium
node test/e2e.mjs            # E2E，預設打正式站
node test/sync-protocol.mjs  # WebSocket 協定層測試
```

要打別的環境可以設 `BASE_URL` 環境變數。兩支測試都會在結束時把自己建立的節點刪掉，不會弄髒畫布。

## 測試環境與結果

2026-08-20 對正式站 https://stytrix-test.stytrix-test.workers.dev 執行，Chromium headless。E2E 共 16 項，全部通過。

### L1 畫布

| 項目 | 步驟 | 預期 | 結果 |
|------|------|------|------|
| L1-1 | 開啟網址 | 看到空白畫布與工具列 | 通過 |
| L1-2 | 點工具列「+ 矩形」 | 畫布出現矩形節點 | 通過 |
| L1-3 | 點「+ 文字」後雙擊節點，輸入「E2E 測試文字」，點空白處結束 | 節點顯示輸入的文字 | 通過 |
| L1-4 | 點一下矩形節點 | 出現藍色選取框 | 通過 |
| L1-5 | 按住矩形拖曳 (+120, +80) | 節點跟著滑鼠移動到對應位置 | 通過，實測位移 (120, 80) |

實作跟原測試腳本有兩個差異。第一，節點是從工具列按鈕產生，會出現在畫布中央附近，不是在畫布上點一下或拖出範圍來放置。第二，文字節點要雙擊才進入編輯，不是建立後直接打字。兩者都符合題目「可新增矩形與文字節點、可用滑鼠拖曳移動、可點選選取」的要求。

另外，腳本裡「L1 重整頁面東西會不見」描述的是還沒接存檔的階段。目前部署的版本已經接上 D1，重整後內容會保留，這個行為由 L2 的測試驗證。

### L2 存檔

| 項目 | 步驟 | 預期 | 結果 |
|------|------|------|------|
| L2-1 | 做完 L1 與 L5 的操作後重整頁面 | 矩形（含拖曳後位置）、文字內容、圖片全部還在 | 通過 |
| L2-2 | 直接查 `GET /api/nodes` | D1 裡有對應節點資料與拖曳後座標 | 通過 |

寫入時機：新增與文字更新即時寫入 D1；拖曳過程只走 WebSocket 廣播，放開滑鼠才把最終座標寫進 D1。

### L3 協作

| 項目 | 步驟 | 預期 | 結果 |
|------|------|------|------|
| L3-1 | 開兩個獨立瀏覽器 context（A、B），A 新增矩形 | B 不重整就看到新節點 | 通過 |
| L3-2 | A 拖曳該節點 | B 畫面上該節點即時移到相同位置 | 通過 |
| L3-3 | A 移動滑鼠 | B 看到 A 的游標與名字（題目加分項） | 通過 |

協定層測試（`test/sync-protocol.mjs`）另外驗證了同一件事：開兩條 WebSocket，A 送 create、move（commit）、cursor，確認 B 全部收到、D1 有寫入、刪除後資料清乾淨。這層測試不經過 UI，用來區分「同步壞了」跟「畫面壞了」。

### L4 登入

| 項目 | 步驟 | 預期 | 結果 |
|------|------|------|------|
| L4-1 | 未登入呼叫 `GET /api/me` | 回 401 | 通過 |
| L4-2 | 呼叫 `GET /api/auth/login` | 302 轉導 accounts.google.com，帶正確 client_id 與 redirect_uri | 通過 |
| L4-3 | 未登入建立節點 | `created_by` 記錄為「訪客」 | 通過 |
| L4-4 | 真人走完 Google 登入 | 畫面右上顯示名字與頭像，之後建立的節點記錄登入者名字 | 通過（手動） |

L4-4 沒辦法自動化，因為測試不能代替真人向 Google 認證。2026-08-20 由帳號擁有者手動驗證：登入後畫面顯示使用者名字與大頭貼。設計上未登入者也能使用畫布，節點建立者記為「訪客」；登入者的節點記錄其 Google 顯示名稱，滑鼠停在節點上會顯示「建立者：某某」。

### L5 上傳

| 項目 | 步驟 | 預期 | 結果 |
|------|------|------|------|
| L5-1 | 把 PNG 檔拖進畫布（測試用 DragEvent + DataTransfer 模擬原生拖放） | 圖片上傳到 R2，畫布出現圖片節點且圖片載入成功 | 通過 |
| L5-2 | 檢查圖片節點的 src | 走 `/api/files/img/...`，也就是從 R2 讀回 | 通過 |
| 重整 | 併入 L2-1 | 重整後圖片節點還在且圖片正常載入 | 通過 |

圖片節點的位置跟其他節點一樣存在 D1，R2 只存檔案本體，節點的 `content` 欄位存 R2 的 key。

### 清理

測試結束時比對 `GET /api/nodes` 在測試前後的差集，把測試建立的 4 個節點透過 WebSocket 刪掉，並確認殘留為 0。

## 測試過程發現並修掉的 bug

編輯文字時直接點畫布空白處，textarea 會被 React 卸載，但卸載不會觸發 blur 事件，所以文字內容不會送到後端，重整後就消失。這個問題是在設計 L1-3 測試流程、逐行核對事件處理時發現的，先修掉才跑測試。修法是在畫布的 pointerdown 先把編輯中的內容送出，再結束編輯狀態。L1-3 與 L2-1 的文字持久化測試覆蓋了這個情境，之後若再壞會被抓到。
